---
interface Props {
  narrow?: boolean;
  wide?: boolean;
  class?: string;
}

const { narrow = false, wide = false, class: className = "" } = Astro.props;
---

<div class:list={["container", { narrow, wide }, className]}>
  <slot />
</div>

<style>
  .container {
    width: 100%;
    max-width: var(--container-max);
    margin-inline: auto;
    padding-inline: var(--space-md);
  }

  .container.narrow {
    max-width: var(--container-narrow);
  }

  .container.wide {
    max-width: var(--container-wide);
  }
</style>
